/** @type {import("prettier").Config} */
const config = {
  arrowParens: 'always',
  endOfLine: 'lf',
  printWidth: 100,
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
}

export default config
